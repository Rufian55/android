<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayMessageActivity"
                  android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required for API level <= 15. -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".Activity_02"
            android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required for API level <= 15. -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_02" />
        </activity>
        <activity android:name=".Activity_03"
            android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required for API level <= 15. -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_03" />
        </activity>
        <activity android:name=".Activity_04"
            android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required for API level <= 15. -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_04" />
        </activity>
        <activity android:name=".Activity_05"
            android:parentActivityName=".MainActivity">
            <!-- The meta-data tag is required for API level <= 15. -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_05" />
        </activity>
    </application>

</manifest>